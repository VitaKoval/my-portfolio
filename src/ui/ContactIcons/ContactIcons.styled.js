import styled from 'styled-components'
import { Flex } from '@/ui'
import css from '@styled-system/css'

export const ContactIconsContainer = styled(Flex)(
  css({
    flexDirection: 'column',
    alignItems: 'center',
    gap: 4,
  }),
)
